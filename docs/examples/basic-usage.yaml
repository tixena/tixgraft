# Basic usage example - pull a Docker configuration
repository: "my_organization/docker-templates"
tag: "main"

pulls:
  - source: "nodejs/production"
    target: "./docker"
    type: "directory"
    commands:
      - "echo 'Docker configuration copied successfully'"
