//! Unit tests for JSON schema validation of tixgraft configuration.
//!
//! These tests validate that `docs/schema.json` correctly accepts or rejects
//! configuration structures, with particular focus on the `context` property
//! at both root and pull levels.

#[cfg(test)]
mod tests;
